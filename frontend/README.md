# Website Factory Frontend Scaffold

This is the first minimal runtime scaffold only.

Implemented now:
- minimal workspace view
- separate rendering for:
  - `prospectStatus`
  - `outreachStatus`
  - `priority`
  - `nextAction`
  - `nextActionDue`
- API call to backend workspace route

Not implemented yet:
- real list/detail workflow
- forms
- filters
- badges beyond text rendering
- production build tooling
